func Surprise(s string) bool {     if len(s) < 1 {         return false 
    }     return surprise_annexe(s[0]) || Surprise(s[1:]) 
}  func surprise_annexe(a byte) bool {     return (a <= 'z' && a >= 'a') || (a <= 'Z' && a >= 'A') }

énoncé :
faite une récursive de l'alphabet minuscule et majuscule (doit afficher l'un ou l'autre),  vérifie si la longeur dépasse 1 
